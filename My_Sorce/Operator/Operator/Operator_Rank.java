package Operator;

public class Operator_Rank {
	public static void main(String[] args) {
																
		
		/* 														연산방향
		증감(++,--), 부호(+,-), 비트(~), 논리(!)						<----------------
		산술(*,/,%)												---------------->
		쉬프트(<<, >>, >>>)										---------------->
		비교(<, >, <=, >=, instanceof)							---------------->
		비교(==, !=)												---------------->
		논리(&)													---------------->
		논리(^)													---------------->
		논리(|)													---------------->
		논리(&&)													---------------->
		논리(||)													---------------->
		논리(?:)													---------------->
		대입(=, +=, -=, *=, /=, %=, &=, ^=, |=, <<=, >>=, >>>=)  <----------------
		*/
		
		
	}
}
